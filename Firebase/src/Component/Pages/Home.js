import React from "react";
// import { Link,useNavigate } from "react-router-dom";
// import { UserAuth } from "../Context/AuthContext";

const Home = () => {
  // const { user } = UserAuth();
  // const navigate = useNavigate();

  return (
    <>
      <div className="main">
          <h1 className="text-info text-center">Welcome to Firebase </h1>
        {/* {user ? (
        ) : (
        )} */}
      </div>
    </>
  );
};

export default Home;
